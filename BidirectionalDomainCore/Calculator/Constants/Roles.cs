namespace Bidirectional.DomainCore.Calculator.Constants;

/// <summary>
/// Application role names used for authorization (e.g. Administrator).
/// Base class for role constants to support extension.
/// </summary>
public abstract class Roles
{
    /// <summary>Administrator role name.</summary>
    public const string Administrator = nameof(Administrator);
}

namespace Bidirectional.DomainCore.Common.Enums;

/// <summary>
/// Represents the unit of a value (calculator module).
/// </summary>
public enum ValueUnit
{
    Percentage = 0,
    Dollar = 1,
    Year = 2,
    Month = 3,
    Day = 4,
    None = 5,
}
